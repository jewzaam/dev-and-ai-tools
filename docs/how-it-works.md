<!-- Generated By: Claude Code (Claude Opus 4.6) -->

# How It Works

## The Big Picture

You do four things. The agents do everything else.

### Step 1: Have requirements

Get a `docs/requirements.md` into your project. This could come from anywhere — write it
yourself, generate it from a brainstorming skill, pull it from a spec-kit session, or
paste in notes from a conversation. The format doesn't matter as long as it describes
what you want built.

### Step 2: Generate architecture and tasks

Run `/project-bootstrap` (or `/project-bootstrap docs/requirements.md` if the file is
somewhere else). This produces `docs/ARCHITECTURE.md` (tech stack, data models, API
design, deployment) and `docs/TASKS.md` (ordered task breakdown with acceptance criteria).
**Review both files and make corrections before proceeding** — everything downstream
builds on these.

### Step 3: Scaffold the workflow

Run `/agentic-scaffold docs/TASKS.md docs/ARCHITECTURE.md`. This generates task specs,
copies personas, creates `orchestrator.yaml` and `loop.sh`. Optionally edit
`orchestrator.yaml` to change workflow mode, model routing, or quality gates.

### Step 4: Run the loop and walk away

Run `/agentic-loop`. It validates prerequisites, then runs `loop.sh` unattended until
all tasks are approved or one gets blocked. When it finishes, review the git history
and push.

```mermaid
graph TD
    YOU(("Human")) --> R["1: requirements.md"]
    R -->|/project-bootstrap| A["2: Review docs"]
    A -->|/agentic-scaffold| S["3: Edit config"]
    S -->|/agentic-loop| L["4: Walk away"]
```

## Workflow Modes

The build loop supports two modes, controlled by `orchestrator.yaml`:

### TDD Mode (default)

Tests are written first. The developer's job is to make them pass.

```mermaid
graph TD
    TEST["🧪 TEST<br/><i>Write tests against<br/>architecture interfaces</i>"]
    DEV["💻 DEV<br/><i>Make tests pass</i>"]
    REVIEW["🔍 REVIEW<br/><i>Run /review skill</i>"]
    JUDGE{"⚖️ JUDGE<br/><i>Score on 6 dimensions<br/>Triage review findings</i>"}
    VERIFY["✅ VERIFY<br/><i>Run test suite</i>"]
    NEXT["Next task"]
    BLOCKED["🚫 BLOCKED<br/><i>Human intervention</i>"]

    TEST --> DEV
    DEV --> REVIEW
    REVIEW --> JUDGE
    JUDGE -->|PASS| VERIFY
    JUDGE -->|FAIL + mandated_fixes| DEV
    JUDGE -->|3 failures| BLOCKED
    VERIFY -->|tests pass| NEXT
    VERIFY -->|"tests fail<br/>(unchecks DEV + REVIEW)"| DEV
```

### Standard Mode

Developer writes code first, tests come after the judge approves.

```mermaid
graph TD
    DEV2["💻 DEV<br/><i>Implement spec</i>"]
    REVIEW2["🔍 REVIEW<br/><i>Run /review skill</i>"]
    JUDGE2{"⚖️ JUDGE<br/><i>Score + triage</i>"}
    TEST2["🧪 TEST<br/><i>Write adversarial tests</i>"]
    VERIFY2["✅ VERIFY<br/><i>Run test suite</i>"]
    NEXT2["Next task"]
    BLOCKED2["🚫 BLOCKED"]

    DEV2 --> REVIEW2
    REVIEW2 --> JUDGE2
    JUDGE2 -->|PASS| TEST2
    JUDGE2 -->|FAIL + mandated_fixes| DEV2
    JUDGE2 -->|3 failures| BLOCKED2
    TEST2 --> VERIFY2
    VERIFY2 -->|tests pass| NEXT2
    VERIFY2 -->|"tests fail<br/>(unchecks DEV + REVIEW)"| DEV2
```

## What Each Persona Does

```mermaid
sequenceDiagram
    participant TL as TASK_LIST.md
    participant TW as Test Writer
    participant D as Developer
    participant R as Reviewer
    participant J as Judge
    participant V as Verify

    Note over TL: → NEXT: Task 1.1 — TEST

    TL->>TW: Read spec + ARCHITECTURE_REF.md
    TW->>TW: Write tests (expected to fail)
    TW->>TL: Commit, advance cursor

    TL->>D: Read spec + existing tests
    D->>D: Make tests pass
    D->>TL: Commit, advance cursor

    TL->>R: Read spec + dev diff
    R->>R: Run /review skill
    Note over R: Review-*.md<br/>C0,C1 (Critical)<br/>I0,I1 (Important)
    Note over R: Review-*-supplementary.md<br/>S0,S1 (Suggestions)<br/>Strengths, detailed analysis
    R->>R: Copy both files to tasks/feedback/
    R->>TL: Commit review, advance cursor

    TL->>J: Read spec + diff + both review files
    J->>J: Score 6 dimensions
    J->>J: Triage review findings
    J->>J: Run xenon (if available)

    alt PASS
        J->>TL: Advance to VERIFY
        TL->>V: Run test suite
        V->>TL: Advance to next task
    else FAIL
        Note over J: mandated_fixes:<br/>- C0: SQL injection<br/>- Complexity: CC=14
        J->>TL: Loop back to DEV with fixes
        TL->>D: Read mandated_fixes, fix them
    end

    J->>J: Append to FEEDBACK.md
```

## Judge Scoring

The judge scores every task on 6 dimensions:

```mermaid
graph TD
    subgraph "Blocking Thresholds"
        D1["Spec Compliance<br/>❌ if < 4/5"]
        D2["Implementation Quality<br/>❌ if < 3/5"]
        D3["Test Quality<br/>❌ if < 3/5"]
        D4["Code Reuse<br/>❌ if < 3/5"]
        D5["Slop Detection<br/>❌ if < 3/5"]
        D6["Complexity<br/>❌ if < 3/5"]
    end

    D1 --> V{Any blocking?}
    D2 --> V
    D3 --> V
    D4 --> V
    D5 --> V
    D6 --> V

    V -->|No| PASS[PASS or<br/>PASS_WITH_CONCERNS]
    V -->|Yes| FAIL[FAIL<br/>+ mandated_fixes list]
```

## Review → Judge → Fix Flow

The reviewer finds issues. The judge decides which must be fixed.

```mermaid
sequenceDiagram
    participant R as Reviewer
    participant RM as Review-*.md
    participant RS as Review-*-supplementary.md
    participant J as Judge
    participant D as Developer

    R->>R: Run /review skill
    R->>RM: Skill writes Critical (C0, C1) + Important (I0, I1)
    R->>RS: Skill writes Suggestions (S0, S1) + strengths
    R->>R: Copy both to tasks/feedback/

    J->>RM: Read main findings
    J->>RS: Read supplementary

    Note over J: For each finding:<br/>Must fix? → mandated_fixes<br/>Acceptable? → Concerns

    alt Findings require fixes
        J->>D: FAIL with mandated_fixes
        Note over D: Must address every<br/>item in mandated_fixes
        D->>D: Fix, recommit
    else All acceptable
        J->>J: PASS (log concerns)
    end
```

## Plugin Component Map

```mermaid
graph LR
    subgraph "Plugin (installable)"
        PJ[plugin.json<br/>marketplace.json]

        subgraph "Skills"
            PB[/project-bootstrap/]
            AS[/agentic-scaffold/]
            AL[/agentic-loop/]
        end

        subgraph "Personas"
            DEV[developer.md]
            TW[test_writer.md]
            REV[reviewer.md]
            JDG[judge.md]
        end

        subgraph "Templates"
            OY[orchestrator.yaml]
            LS[loop.sh]
            TLM[TASK_LIST.md]
            RM[RUN.md]
            BS[BUILD_STATUS.md]
        end

        subgraph "Container"
            CF[Containerfile]
            SB[run-claude-sandbox.sh]
            VL[validate-loop-prereqs.sh]
        end
    end

    subgraph "External Dependencies"
        RS["/review skill<br/>(required)"]
        XN["xenon<br/>(optional)"]
        PM["Podman<br/>(required)"]
    end

    REV -.->|runs| RS
    JDG -.->|runs| XN
    SB -.->|uses| PM
    AL -.->|calls| VL
    AL -.->|runs| LS
    AS -.->|copies| DEV
    AS -.->|copies| TW
    AS -.->|copies| REV
    AS -.->|copies| JDG
    AS -.->|copies| OY
    AS -.->|copies| LS
    PB -.->|produces docs for| AS
```

## Model Routing

Each stage uses the model best suited to its task, configured in `orchestrator.yaml`:

```mermaid
graph LR
    subgraph "Sonnet (fast, cheap)"
        T[TEST]
        D[DEV]
        R[REVIEW]
    end

    subgraph "Opus (deep reasoning)"
        J[JUDGE]
    end

    L[loop.sh] -->|reads orchestrator.yaml| T
    L --> D
    L --> R
    L --> J
```

Override all stages with `./loop.sh --model sonnet`.

## Final Judge

After all tasks pass, a separate FINAL JUDGE reviews the entire project — not a single task.

```mermaid
graph TD
    TASKS["All tasks<br/>PASS"] --> FJ{"FINAL JUDGE<br/><i>Reviews git log,<br/>all judge verdicts,<br/>FEEDBACK.md</i>"}
    FJ -->|PASS| APPROVED["BUILD_STATUS.md<br/>= APPROVED"]
    FJ -->|FAIL| FAILED["BUILD_STATUS.md<br/>= FAILED"]
```

The final judge looks for systemic issues per-task judges may have missed: inconsistent
patterns across phases, unresolved concerns from multiple `pass_with_concerns` verdicts,
and review quality trends.

## State Model

All state lives in the filesystem. There is no in-memory persistence between containers.
Each stage runs in a fresh Podman container that reads state, does work, writes state, and
exits. `loop.sh` on the host reads the updated state to decide what to do next.

```mermaid
graph LR
    subgraph "Filesystem State"
        TL["tasks/TASK_LIST.md<br/><i>Cursor position<br/>Checkbox state<br/>Attempt counters</i>"]
        BS["tasks/BUILD_STATUS.md<br/><i>PENDING / APPROVED / FAILED</i>"]
        FB["tasks/feedback/<br/><i>Judge verdicts<br/>Review output<br/>Verify logs</i>"]
        FBM["tasks/FEEDBACK.md<br/><i>Accumulated feedback<br/>across all iterations</i>"]
    end
```

## Container Isolation

Each stage runs inside a hardened Podman container. The container is the security boundary.

| Control | Setting |
|---------|---------|
| Capabilities | `--cap-drop ALL` |
| Privilege escalation | `--security-opt no-new-privileges` |
| Processes | `--pids-limit 256` |
| Network | `--network none` default; `loop.sh` passes `--host-network` for API access |
| Volumes | Worktree (rw) + auth tokens (rw) only |
| Container lifecycle | `--rm` (ephemeral, no state persists in the container) |

### Why no `--read-only` filesystem?

The original design used `--read-only` with targeted `--tmpfs` mounts for directories
Claude Code needs to write to (`/tmp`, `/home/node/.config`, `/home/node/.local`). This
worked on Linux but failed on Windows (Podman for Windows) due to two issues:

1. **Claude Code writes to unpredictable locations.** Beyond the expected XDG directories,
   it writes `/home/node/.claude.json` directly in the home directory, and future versions
   may add more. Maintaining a whitelist of tmpfs mounts is fragile.
2. **Podman on Windows doesn't support `uid`/`gid` tmpfs options.** Without these, tmpfs
   mounts are root-owned and the `node` user (uid 1000) gets `EACCES` errors. The `mode`
   option is also unsupported.

Rather than maintaining platform-specific isolation logic, `--read-only` was removed
entirely. The remaining controls are sufficient:

- **Volume mounts** are the real isolation boundary — only the worktree and auth directory
  are accessible from the host. Writes to the container's root filesystem cannot reach the
  host.
- **`--cap-drop ALL`** prevents privileged operations (mounting filesystems, changing
  ownership of root-owned files, binding privileged ports).
- **`--rm`** makes the container ephemeral — any modifications to the root filesystem
  (e.g., a compromised process replacing system binaries) vanish when the container exits.
- **`--security-opt no-new-privileges`** prevents setuid escalation.

The `--read-only` flag was defense-in-depth against in-session persistence attacks, which
are already mitigated by ephemeral containers.

## Known Limitations

- **No stuck-state detection.** If a container exits without changing `TASK_LIST.md` state
  (API outage, container crash), the loop re-runs the same stage indefinitely. There is no
  iteration counter or timeout in `loop.sh`.
- **Tracing not yet implemented.** `orchestrator.yaml` has a `tracing.enabled` field but
  no code reads it.
