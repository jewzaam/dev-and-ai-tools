<!-- Generated By: Claude Code (Claude Opus 4.6) -->

# Review: C4 Validation — agentic-orchestrator

## Summary

| Artifact | Confirmed | Critical | Important | Minor |
|----------|-----------|----------|-----------|-------|
| l1-c4-context.md | 6 | 0 | 0 | 0 |
| l2-c4-container.md | 5 | 0 | 0 | 1 |
| l3-c4-component.md | 8 | 0 | 1 | 0 |
| behavioral-spec.md | 12 | 1 | 1 | 1 |
| **Total** | **31** | **1** | **2** | **2** |

## Critical

### C0: Network default blocks agentic loop API access

**Artifact:** behavioral-spec.md
**Section:** 10 — Behavioral Nuances
**Verdict:** RESOLVED

**Original issue:** Default network mode was `--network none`, blocking API access.
`loop.sh` invoked `run-claude-sandbox.sh` without a network flag.

**Resolution:** Default changed to `--network host`. `loop.sh` no longer needs an
explicit network flag. The `--host-network` flag was removed; `--no-network` added
as opt-in for offline commands.

## Important

### I0: Loop has no stuck-state detection

**Artifact:** behavioral-spec.md
**Section:** 10 — Behavioral Nuances
**Verdict:** MISSING

**Spec says:** If a container exits without changing TASK_LIST.md state, the loop re-runs
the same stage indefinitely.

**Code says:** `loop.sh:54-99` — the while loop has no iteration counter or timeout.
Exit conditions are only `APPROVED`, `FAILED*`, `[BLOCKED]`, or empty cursor.

**Evidence:** Direct read of `loop.sh:54-99`. No variable tracks total iterations.

**Impact:** API outage, container crash, or misconfiguration causes an infinite loop
that consumes resources until manually killed.

### I1: TASK_LIST.md REVIEW entry references stale skill set — RESOLVED

**Artifact:** l3-c4-component.md
**Section:** Data Flow
**Verdict:** RESOLVED

Fixed during this session. `TASK_LIST.md:68` now reads "Run /review skill (or manual
review if unavailable)." Consistent with reviewer persona.

## Minor

### M0: Tracing config is a dead field

**Artifact:** behavioral-spec.md
**Section:** 10 — Behavioral Nuances
**Verdict:** MISSING

**Spec says:** `orchestrator.yaml` includes `tracing.enabled`.

**Code says:** No code reads this value. `loop.sh` reads only `models.*` keys.
No persona references tracing.

**Evidence:** `grep -r tracing` across all shell and markdown files returns only the
orchestrator.yaml template itself.

**Impact:** Low — it is documented as a placeholder. A user setting `tracing.enabled: true`
would see no effect.

### M1: loop.sh read_yaml is fragile for nested YAML

**Artifact:** l2-c4-container.md
**Section:** State Management
**Verdict:** MISSING

**Spec says:** `loop.sh` reads `orchestrator.yaml` for model routing.

**Code says:** `read_yaml` at `loop.sh:20-30` uses `grep` + `sed` to parse YAML.
It matches `key:` with leading whitespace, which works for the flat `models:` section
but would match wrong values if duplicate key names appeared at different nesting levels.

**Evidence:** Direct read of `loop.sh:20-30`. The grep pattern `"^  *${key}:"` matches
any indented line with that key name regardless of its YAML parent.

**Impact:** Low with the current orchestrator.yaml structure (no duplicate key names across
sections). Would break if a future config added a key named `dev` or `judge` under a
different section.
